.section.dark_style
    .container
        .section__title Back#[span(class="section__title--bold") -Office]
        .section__subtitle A feature-rich back-office to maintain matching engine #[span(class="db")] functionality
    
    .trading
        .trading__title Balances
        .trading__slider.flickity_castom_arrow
            .item
                img.banner(src="images/balans1.png", alt="banner")
                .trading__flex
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Displaying balances in the context of currencies
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Showing equivalent of each user’s currencies in USD equivalent
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Separate locked balance monitoring
            
            .item
                img.banner(src="images/balans1.png", alt="banner")
                .trading__flex
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Filter by date range
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Filter by asset
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Filter by transfer type
                    
            .item
                img.banner(src="images/balans1.png", alt="banner")
                .trading__flex
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Time sorted table of transfers
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Various filters and sorting available
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Amount viewing in USD
            
            .item
                img.banner(src="images/balans1.png", alt="banner")
                .trading__flex
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Displaying aggregated balances in USD
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Export of transfers details in XLS table format
                    
                    .check_block--mam_platform-item.circle
                        +icon('circle')
                        .check_block--mam_platform-item--text Viewing all asset amounts in one place 