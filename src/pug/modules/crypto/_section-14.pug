.section-14.section__crypto_market.section
  .container.goanim.an
    .section__title Market #[span(class="section__title--bold") Depth] #[span(class="db")] and #[span(class="section__title--bold") Liquidity] pool
    .main-text Market liquidity impacts everything from the bid-offer spread to trade execution and is important since it affects how quickly you can open and close positions. As a broker, you need to have access to the deepest liquidity pool available to handle all the needs of your traders.
    .image_block
      img(src="images/new_table.png", alt="table")
    .check_container
      .check_block--mam_platform-item.circle
        +icon('circle')
        span.check_block--mam_platform-item--text Aggregated liquidity with lots of volume even#[span(class="db")] on first tiers of the depth allows traders#[span(class="db")] to get their trades with minimal slippage
      .check_block--mam_platform-item.circle
        +icon('circle')
        span.check_block--mam_platform-item--text Aggregation provides competitive spreads for all instruments
      .check_block--mam_platform-item.circle
        +icon('circle')
        span.check_block--mam_platform-item--text Aggregation means a strong backup structure behind it